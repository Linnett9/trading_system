backtest:
  provider: stooq_parquet
  data_dir: data/processed/stooq_parquet

ml:
  enabled: true
  mode: research
  model_type: patchtst
  shadow_model_type: patchtst
  label_type: should_reduce_exposure
  prediction_target: should_reduce_exposure
  feature_set: expanded_rebalance_v1
  research_label: EXPANDED_REBALANCE_CURRENT_32_RESEARCH_ONLY
  minimum_history_years: 4
  output_dir: reports/ml/patchtst_should_reduce_exposure
  benchmark_symbols:
    - SPY
    - QQQ
  sequence_length: 64
  patchtst_sequence_length: 64
  patchtst_patch_length: 8
  patchtst_patch_stride: 4
  patchtst_d_model: 64
  patchtst_heads: 4
  patchtst_layers: 2
  patchtst_feedforward: 128
  patchtst_dropout: 0.15
  patchtst_epochs: 30
  patchtst_batch_size: 64
  patchtst_learning_rate: 0.001
  patchtst_weight_decay: 0.0005
  patchtst_pos_weight: auto
  comparison_models:
    - patchtst
  overlay_comparison_models:
    - patchtst
  expanded_rebalance_audit_path: reports/ml/expanded_rebalance_dataset_audit.json
  expanded_rebalance_dataset:
    rebalance_frequencies:
      - monthly
      - biweekly
      - weekly
    top_n_values:
      - 3
      - 5
      - 7
    weightings:
      - equal
      - inverse_volatility
    universe_paths:
      - data/reference/universes/current_32.yaml
      - data/reference/universes/us_liquid_100.yaml
    reduce_drawdown_threshold: 0.08
    reduce_excess_return_threshold: -0.01
    reduce_volatility_adjusted_threshold: -0.10
