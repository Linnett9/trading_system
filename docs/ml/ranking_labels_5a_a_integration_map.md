# Ticket 5A-A Deferred Integration Map

All registry and model integrations are marked:

`DEFER UNTIL ACTIVE SELECTOR PUBLICATION AND COMPONENT-READINESS WORK FINISHES`

| Future integration | Required gate |
|---|---|
| Ranking-contract registry | Review percentile, pairwise and grouped-dataset identities after active readers finish |
| Target-contract registry | Bind exact forward-return target and maturity identity |
| Authoritative selector dataset | Frozen feature, row, date, split and target checksums |
| LightGBM Rank-XENDCG | Dependency audit and explicit integer/continuous label support |
| LightGBM LambdaRank | Nonnegative integer relevance and exact group-size vector |
| XGBoost Rank-NDCG | Verified qid vector and objective-compatible relevance |
| XGBoost Pairwise | Separate pairwise dataset and feature-difference adapter |
| Bounded strict-OOS fitting | No group deletion; training-only transformations |
| Experiment search accounting | Count every objective, configuration, seed, failure and rejection |
| Protected research protocol | Frozen development panel and protected final audit |
| Ranking metrics and promotion reports | Matched NDCG, Rank IC, turnover, continuity and after-cost evidence |

The next ranking-model ticket must not silently convert continuous percentile
labels to integer relevance or flatten pairwise evidence into a row-wise label.

