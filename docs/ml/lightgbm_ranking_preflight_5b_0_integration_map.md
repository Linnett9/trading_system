# Ticket 5B-0 Deferred Integration Map

Next owner:

**Ticket 5B-A — LightGBM Rank-XENDCG Synthetic Selector Foundation**

It will consume:

| Input | Deferred use |
|---|---|
| Ticket 5A-A grouped export | Frozen features, labels, group sizes and row identities |
| LightGBM 4.6.0 | Exact dependency capability established here |
| Deterministic CPU policy | One or two threads and fixed histogram/seeding settings |
| Ticket 0.2B | Experiment-search accounting |
| Ticket 0.3A | Research budgets and protected protocol |

All selector-data, fitting-runner, registry, replay, promotion and live-data
integration is marked:

`DEFER UNTIL ACTIVE SELECTOR PUBLICATION AND COMPONENT-READINESS WORK FINISHES`

Ticket 5B-A must remain synthetic and bounded until that directive is lifted.
