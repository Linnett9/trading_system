ml:
  stock_alpha_stock_rows_path: reports/ml/benchmark/regime_transformer_meta_ensemble_v1/stock_alpha_ensemble_diagnostic/dev/ensemble/average_rank/stock_alpha_ensemble_average_rank_predictions.csv
  stock_alpha_news_collection_plan_report_dir: reports/ml/benchmark/regime_transformer_meta_ensemble_v1/stock_alpha_news_collection_plan_real/dev
  stock_alpha_news_provider_audit_config_path: config/config.stock_alpha_news_provider_audit_real_template.yaml
  stock_alpha_news_collect:
    symbols: &broader_symbols [AAPL, MSFT, NVDA, AMZN, GOOGL, META, TSLA, AVGO, BRK.B, JPM, V, MA, XOM, UNH, COST, HD, PG, JNJ, ABBV, NFLX, CRM, AMD, ORCL, BAC, KO, PEP, WMT, CVX, MRK, CSCO]
    max_articles_per_provider: 100
    providers:
      gdelt: {enabled: false}
      alpha_vantage: {enabled: true, api_key_env: ALPHA_VANTAGE_API_KEY}
      finnhub: {enabled: false, api_key_env: FINNHUB_API_KEY}
      fmp: {enabled: false, api_key_env: FMP_API_KEY}
      newsapi: {enabled: false, api_key_env: NEWSAPI_API_KEY}
  stock_alpha_news_enable_transformer: false
  research_only: true
  trading_impact: none
  production_validated: false
  promotion_thresholds_changed: false
