ml:
  stock_alpha_news_collect_report_dir: reports/ml/benchmark/regime_transformer_meta_ensemble_v1/stock_alpha_news_collect_alpha_vantage_broader_write/dev
  stock_alpha_news_collect_output_path: data/news/raw/stock_alpha_news_provider_export.csv
  stock_alpha_news_collect:
    enabled: true
    dry_run: false
    allow_overwrite: false
    max_articles_per_provider: 100
    request_timeout_seconds: 20
    start_date: '2025-01-01'
    end_date: '2025-03-31'
    symbols: [AAPL, MSFT, NVDA, AMZN, GOOGL, META, TSLA, AVGO, BRK.B, JPM, V, MA, XOM, UNH, COST, HD, PG, JNJ, ABBV, NFLX, CRM, AMD, ORCL, BAC, KO, PEP, WMT, CVX, MRK, CSCO]
    providers:
      gdelt: {enabled: false}
      alpha_vantage: {enabled: true, api_key_env: ALPHA_VANTAGE_API_KEY}
      finnhub: {enabled: false, api_key_env: FINNHUB_API_KEY}
      fmp: {enabled: false, api_key_env: FMP_API_KEY}
      newsapi: {enabled: false, api_key_env: NEWSAPI_API_KEY}
  stock_alpha_news_enable_transformer: false
  research_only: true
  trading_impact: none
  production_validated: false
  promotion_thresholds_changed: false
